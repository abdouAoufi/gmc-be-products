export const getAllProducts = (req, res, _next) => {
  res.send({ message: "Not Implemented Yet" });
};
export const getSingleProduct = (req, res, _next) => {
  res.send({ message: "Not Implemented Yet" });
};
export const createProduct = (req, res, _next) => {
  res.send({ message: "Not Implemented Yet" });
};
